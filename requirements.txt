# ── Scraper Service — Python Dependencies ────────────────────────────────────
# Python 3.14+ compatible (pre-built wheels only)
#
# Install:
#   python3 -m venv venv
#   venv/bin/pip install -r requirements.txt --only-binary=:all:
#   venv/bin/playwright install chromium

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi>=0.115.5
uvicorn[standard]>=0.32.1
pydantic>=2.10.1

# ── HTTP clients ──────────────────────────────────────────────────────────────
requests==2.32.3
requests-toolbelt==1.0.0
httpx==0.27.2

# ── HTML scraping ─────────────────────────────────────────────────────────────
beautifulsoup4==4.12.3
cloudscraper==1.2.71

# ── Headless browser ─────────────────────────────────────────────────────────
playwright>=1.49.0
selenium>=4.26.1
webdriver-manager>=4.0.2

# ── PDF extraction ────────────────────────────────────────────────────────────
pdfminer.six==20221105

# ── Excel + image export ──────────────────────────────────────────────────────
openpyxl==3.1.5
Pillow>=11.0.0

# ── Character encoding ────────────────────────────────────────────────────────
charset-normalizer==3.4.0
